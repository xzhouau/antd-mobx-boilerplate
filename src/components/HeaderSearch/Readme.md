HeaderSearch component example:

```jsx
<HeaderSearch
  placeholder="Search"
  dataSource={[]}
  onChange={() => { console.log('Search input changed') }}
  onPressEnter={() => { console.log('Enter pressed') }}
/>
```
